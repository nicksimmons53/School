#!/usr/bin/env python3

pi = 3.14
radius = 3
circumference = 2 * pi * radius

# ints must be converted to strings to concatenate them
msg = "c = 2 * " + str(pi) + " * " + str(radius)

print(msg)
print("c = " + str(circumference))
